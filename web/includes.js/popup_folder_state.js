function popup_folder_state(device_id, state)
	{
	onload = function(data)
		{
//		document.getElementById(device_id + '_1').style.display = ['', 'none'][data];
//		document.getElementById(device_id + '_0').style.display = ['none', ''][data];
		}

//	ajax({ type : "GET", url : "index.php?Cmd=" + ["DeviceBlockOff", "DeviceBlockOn", "DeviceBlockState"][state] + "&DeviceId=" + device_id, success : onload });
	}

